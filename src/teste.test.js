describe("Inicializando processos de testes", () => {
  // Este arquivo foi criado apenas para validar
  // se os testes estão sendo executados.

  test("este teste irá passar normalmente", () => {
    expect(1 + 1).toBe(2);
  });
});
